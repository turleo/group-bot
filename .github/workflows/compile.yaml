name: 🏷️ Check types
on: [push]

jobs:
  build:
    name: Build TypeScript to check types
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run typecheck
